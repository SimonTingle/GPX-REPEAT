name: Semantic Version Bump

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'CHANGELOG.md'
      - 'package.json'

jobs:
  version-bump:
    runs-on: ubuntu-latest

    # Prevent infinite loops from auto-commits
    if: ${{ !contains(github.event.head_commit.message, 'chore: bump version via GitHub Actions') }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history to parse all commits
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Configure git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Run version bump script
        id: version-bump
        run: node scripts/bump-version.js
        continue-on-error: true

      - name: Check if changes were made
        id: check-changes
        run: |
          if git diff --quiet; then
            echo "has-changes=false" >> $GITHUB_OUTPUT
          else
            echo "has-changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.check-changes.outputs.has-changes == 'true'
        run: |
          git add package.json README.md CHANGELOG.md
          git commit -m "chore: bump version via GitHub Actions"
          git push origin main

      - name: Push tags
        if: steps.check-changes.outputs.has-changes == 'true'
        run: git push --tags || true

      - name: Create GitHub Release
        if: steps.check-changes.outputs.has-changes == 'true'
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.NEW_VERSION_TAG }}
          release_name: Release ${{ env.NEW_VERSION_TAG }}
          draft: false
          prerelease: false
